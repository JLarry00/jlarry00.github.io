# Juan Larrondo — personal website

Bilingual personal website built with Astro and plain CSS.

## Local development

Requires Node.js 22.12 or newer.

```sh
npm install
npm run dev
```

Useful checks:

```sh
npm run check
npm run build
```

The production build is fully static and can be deployed to GitHub Pages later.
